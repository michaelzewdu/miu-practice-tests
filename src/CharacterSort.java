import java.util.Arrays;

// Feb 25 2023GC
// Michael Zewdu
public class CharacterSort {

    char[] f(char[] a, int start, int len){
        char[] sortedCharacters = {};
        if (len> a.length ){

        }
        return Arrays.copyOfRange(a,start,len);
    }
}
